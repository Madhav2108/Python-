import random
first_ball_run = random.randrange(6)+1;
second_ball_run = random.randrange(6)+1;
total_run = first_ball_run + second_ball_run;
print("Dhoni hits last two balls and gets total of",total_run,"runs");
print("First ball:",first_ball_run);
print("Second ball:",second_ball_run);
